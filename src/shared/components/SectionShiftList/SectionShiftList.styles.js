const SectionShiftListStyles = theme => ({
  title: {
    justifyContent: 'flex-end',
    fontSize: theme.size.unit14,
    fontWeight: theme.palette.subHeaderText.fontWeight,
    textAlign: 'left',
    color: theme.palette.subHeaderText.color,
    marginTop: theme.spacing.unit28,
    marginLeft: theme.spacing.unit20,
    marginBottom: theme.spacing.unit8,
  },
});

export default SectionShiftListStyles;
