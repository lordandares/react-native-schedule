export type State = {
  showEnvironmentSelector: false
};

export type Props = {
  styles: any,
  loading: boolean,
  register: Function,
  login: Function,
  logout: Function,
  testFlag: boolean,
};
