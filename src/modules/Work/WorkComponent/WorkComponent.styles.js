const workComponentStyles = theme => ({
  container: {
    flex: 1,
    backgroundColor: theme.palette.common.white,
  },
  datePicker: {
    zIndex: -1,
    position: 'absolute',
  },
});

export default workComponentStyles;
